# Certificate and replay

The JSON certificate contains all 22,093 stable complement keys, independent
census counts, character checks through eight moved points, the complete
stable histogram, 1,251 \(q_6\) witnesses, 23 \(q_7\) witnesses, two
\(q_8\) witnesses, 311 odd specializations, and every boundary residual.

For every \(n=9\) deepest collision, it stores both full immanantal
polynomials and the independently computed separating difference.

```sh
uv run laplacian-hook-eleven-check
```

Replay regenerates every value; the JSON is not a trusted witness database.
